layout(location = 0) out vec4 out_color;
void main()
{
	vec2 uv = calcUV();
	vec4 base_color = calcBaseColor(uv);
	vec3 normal = calcNormal(uv);
	vec4 light_color = calcLightColor(normal, frag_world_pos);
	out_color = base_color * light_color;
}